<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.unity.game.mapper.UserInfoMapper">

    <select id="searchUserInfo" parameterType="string" resultType="integer">
        SELECT COUNT(*)
        FROM TBL_UNT_USERINFO_MT01 tuu
        WHERE tuu.PID = #{param}
          AND D_FLAG = 'N'
    </select>

    <insert id="insertUserInfo" parameterType="map">
        INSERT INTO TBL_UNT_USERINFO_MT01 (PID,PNM,PSEX)
        values (#{PID},#{PNM},#{PSEX})
    </insert>

    <select id="getUserInfo" parameterType="string" resultType="map">
        SELECT PID AS pid ,
               PNM AS pnm ,
               PSEX AS psex ,
               CASH AS cash ,
               PLVL AS plvl ,
               MAXHP AS maxhp ,
               CHP AS chp
        FROM TBL_UNT_USERINFO_MT01 tuu
        WHERE tuu.PID =#{param}
          AND D_FLAG = 'N'
    </select>

    <insert id="setDstate" parameterType="map">
        INSERT INTO TBL_UNT_MBTISTATS_NT01 (PID, D_NAME, I, E, S, N, T, F, J, P, MOOD, STREES, FATIGUE, AGE)
             VALUES(#{pid},#{name},#{I},#{E},#{S},#{N},#{T},#{F},#{J},#{P},#{mood} ,#{stress} ,#{fatigue}, #{age} )
    </insert>

    <insert id="setChatLong" parameterType="map">
        INSERT INTO  TBL_UNT_GPTLOG_NT01(PID, USER_MENT, GPT_MENT)
             VALUES(#{pid},#{userment},#{gptment})
    </insert>
</mapper>