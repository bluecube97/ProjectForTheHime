<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.unity.game.mapper.UserInfoMapper">

    <select id="searchUserInfo" parameterType="string" resultType="integer">
        SELECT COUNT(*)
        FROM TBL_UNT_USERINFO_MT01 tuu
        WHERE tuu.PID = #{param}
          AND D_FLAG = 'N'
    </select>

    <insert id="insertUserInfo" parameterType="map">
        INSERT INTO TBL_UNT_USERINFO_MT01 (PID,PNM,PSEX)
        values (#{PID},#{PNM},#{PSEX})
    </insert>

    <select id="getUserInfo" parameterType="string" resultType="map">
        SELECT tuu.PID AS pid,
               tuu.PNM AS pnm,
               tuu.PSEX AS psex,
               tuu.CASH AS cash,
               tuu.PLVL AS plvl,
               tumn.MAXHP AS maxhp,
               tumn.CHP AS chp
        FROM TBL_UNT_USERINFO_MT01 tuu
                 INNER JOIN TBL_UNT_DSTATS_NT01 tumn
                         ON tuu.PID = tumn.PID
        WHERE tuu.PID = 'user10@naver.com'
          AND tuu.D_FLAG = 'N';
    </select>

    <insert id="setDstate" parameterType="map">
        INSERT INTO TBL_UNT_DSTATS_NT01 (PID, D_NAME, I, E, S, N, T, F, J, P, MOOD, STRESS, FATIGUE, AGE, MBTI, SEX)
             VALUES(#{pid},#{name},#{I},#{E},#{S},#{N},#{T},#{F},#{J},#{P},#{mood} ,#{stress} ,#{fatigue}, #{age}, #{mbti}, #{sex})
    </insert>


</mapper>